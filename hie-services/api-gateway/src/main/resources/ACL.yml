roles:
  # Login JWT token roles are "administrator" and "user"
  - roleName: "fhir-user"
    resources:
      /R4/Patient: [ "GET" ]
      /R4/Condition: ["GET", "POST", "PUT"]
      /ingest: [ "POST" ]

  - roleName: "app-user"
    resources:
      /R4/Patient: ["GET", "POST", "PUT"]
      /ingest: ["GET","POST"]